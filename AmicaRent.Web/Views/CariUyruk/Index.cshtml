@model IEnumerable<WebApplication.DataAccess.CariUyruk>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Uyruk Adı
        </th>
        <th>
            Oluşturma Zamanı
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CariUyruk_Adi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CariUyruk_CreateDate)
            </td>
            <td>
                @Html.ActionLink("Düzenle", "Edit", new { id = item.CariUyruk_ID }) |
                @Html.ActionLink("Detaylar", "Details", new { id = item.CariUyruk_ID })
            </td>
        </tr>
    }

</table>
<script type="text/javascript">document.addEventListener("DOMContentLoaded", function () {
    var menuItem = document.querySelector('#solmenu-cari-liste');
        menuItem.classList.add('active');
    var subMenuItem = menuItem.querySelector('#solmenu-cari-liste-CariUyruk');
        subMenuItem.classList.add('active');
    });</script>