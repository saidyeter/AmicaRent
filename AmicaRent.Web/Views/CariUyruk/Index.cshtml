@model IEnumerable<WebApplication.DataAccess.CariUyruk>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CariUyruk_Adi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CariUyruk_Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CariUyruk_CreateDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CariUyruk_Adi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CariUyruk_Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CariUyruk_CreateDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CariUyruk_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CariUyruk_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CariUyruk_ID })
        </td>
    </tr>
}

</table>
