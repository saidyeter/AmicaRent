@model IEnumerable<WebApplication.DataAccess.AracRenk>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AracRenk_Adi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AracRenk_Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AracRenk_CreateDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AracRenk_Adi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AracRenk_Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AracRenk_CreateDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AracRenk_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AracRenk_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AracRenk_ID })
        </td>
    </tr>
}

</table>
