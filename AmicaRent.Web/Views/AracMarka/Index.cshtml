@model IEnumerable<WebApplication.DataAccess.AracMarka>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Marka Adı
        </th>
        <th>
            Marka Durumu
        </th>
        <th>
            Oluşturma Zamanı
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AracMarka_Adi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AracMarka_Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AracMarka_CreateDate)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.AracMarka_ID }) |
                @Html.ActionLink("Details", "Details", new { id = item.AracMarka_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.AracMarka_ID })
            </td>
        </tr>
    }

</table>
