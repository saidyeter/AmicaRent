@using WebApplication.Extensions
<li class="dropdown user user-menu">
    @if (HttpContext.Current.Session["UserId"] != null)
    {
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            @*<img src="~/Admin/dist/img/user.svg" class="user-image" alt="User Image">*@
            <span class="hidden-xs">@HttpContext.Current.Session["UserName"]</span>
        </a>
        <ul class="dropdown-menu">
            <li class="user-header">
                @*<img src="~/Admin/dist/img/user.svg" class="img-circle" alt="User Image">*@
                <p>
                    <small>@HttpContext.Current.Session["UserName"]</small>
                </p>
            </li>
            @*<li class="user-body">
                <div class="row">
                    <div class="col-xs-4 text-center">
                        <a href="#">Followers</a>
                    </div>
                    <div class="col-xs-4 text-center">
                        <a href="#">Sales</a>
                    </div>
                    <div class="col-xs-4 text-center">
                        <a href="#">Friends</a>
                    </div>
                </div>
            </li>*@
            <li class="user-footer">
                @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "" }))
                {
                    @Html.AntiForgeryToken()
                <div class="pull-left">
                    <a class="btn" href="@Url.Action("ChangePassword","Account")">
                        <i class="fa fa-edit"></i> Şifre Değiştir
                    </a>
                </div>
                    <div class="pull-right">
                        <a href="javascript:document.getElementById('logoutForm').submit()" class="btn btn-default btn-flat">Çıkış Yap</a>
                    </div>
                }
            </li>
        </ul>
    }
    else
    {
        @*<ul class="nav navbar-nav">
            <li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
            <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
        </ul>*@
    }
</li>